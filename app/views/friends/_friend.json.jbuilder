json.extract! friend, :id, :first_name, :last_name, :email, :phone, :twiter, :name, :comment, :age_num, :birth_date, :salary_amt, :isbest, :created_at, :updated_at
json.url friend_url(friend, format: :json)
