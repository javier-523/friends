<% content_for :title, "Edit friend" %>

<%= render "form", friend: @friend %>

<br>
    <%= button_to "Delete", @friend, 
            data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, 
            method: :delete, class: "btn btn-danger me-3" %>
